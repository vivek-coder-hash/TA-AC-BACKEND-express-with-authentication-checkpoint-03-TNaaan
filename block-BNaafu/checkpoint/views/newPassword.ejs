<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
  </head>
  <body>
    
    <main>
      <section class="forgot-sec">
        <div class="container flex justify-center-flex align-center">
          <div class="flex-30">
            <a href="/"></a>
            <a href="/"></a>
            <div>
              <h3 class="ternary-heading">
                set your new password
              </h3>
              <form
                action="/verifyEmail/<%= id %>/setPassword"
                method="POST"
              >
                <% if (error) { %>
                <p><%= error %></p>
                <% } %>
                <input
                  type="email"
                  name="email"
                  id="email"
                  value="<%= data.email %>"
                  class="form-control diseable"
                />
                <input
                  type="password"
                  name="password"
                  class="form-control"
                  placeholder="set new password"
                  required
                />
                <input
                type="password"
                name="confirmpassword"
                class="form-control"
                placeholder="set new password"
                required
              />
            <button type="submit" class="form-control">Set Password</button>
              </form>
            </div>
          </div>
        </div>
      </section>
    </main>
  </body>
</html>